apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: home
  name: home
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: home
  type: ClusterIP

